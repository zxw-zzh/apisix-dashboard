# APISIX Dashboard runtime environment variables

# Application
APP_PORT=8080
APP_HOST=0.0.0.0
NODE_ENV=production

# Backend: APISIX
# Full admin API base (with /apisix/admin path), e.g. https://apisix.example.com/apisix/admin
APISIX_ADMIN_URL=
# Admin API key
APISIX_ADMIN_KEY=
# Public gateway base URL (no trailing slash), e.g. https://gw.example.com
APISIX_GATEWAY_URL=

# Etcd
# Single endpoint (http(s)://host:port). For cluster, prefer ETCD_ENDPOINTS
ETCD_ENDPOINT=
# Optional: comma-separated endpoints when using a cluster
# ETCD_ENDPOINTS=

# Optional: OpenSearch endpoint if your backend stats depend on it
# OPENSEARCH_URL=
